DATABASE_URL='postgresql://username:password@host:port/database_name'
REDIS_URL='redis://username:password@host:port'

JWT_SECRET='abc123def456ghi789jkl012mno345pqrs678tuv901wxyz'

# Email (Resend) — optional. If unset, the app logs reminders instead of
# sending them, which is convenient for local dev.
RESEND_API_KEY=''
EMAIL_FROM='Subscription Tracker <no-reply@subtrack.dev>'

# Shared secret required by /api/cron/* endpoints. Send as
# `Authorization: Bearer <CRON_SECRET>`.
CRON_SECRET=''

NEXT_PUBLIC_DEPLOYMENT_URL='http://localhost:3000'
